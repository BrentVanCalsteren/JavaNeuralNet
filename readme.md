#  Renewing RNN from Scratch in Java project -> goal is implementing other architecture's as well
## Focus right now

- RNN with `tanh` activation and `sigmoid` output.
- hopefully pure java only
- Xavier weight initialisation -> stable training.
- gradiant decent
- cross‑entropy loss – suitable for pattern prediction tasks.
- good code is the goal :p

## why not just updating old code
looking back at the code, it's messy, 
mostly the reason since at the end i tried intigrating gpu accelertion 3 years ago, with not much succes
it's faster to rewrite it
I'll keep the old scr in zip
